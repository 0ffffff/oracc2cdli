"""
Tests for oracc2cdli word conversion.

Workflow: test_word_conversion.py provides reusable test functions; run_word_conversion_tests.py
runs them (unit tests always; dataset tests require data/word_level.csv from preprocessing:
load_to_db → build_word_table → export_word_level).
"""
